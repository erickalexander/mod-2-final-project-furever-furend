<%if @user.dogs.count == 0%>
  <p>You have no saved dogs yet!</p>
<%else%>
  <%@user.dogs.each do |dog|%>
    <%=link_to dog.breed, dog_path(dog)%>
  <% end %>
<%end%>

<form>
  <button formaction="/users/<%=@user.id%>/edit">Update Personal Info</button>
</form>
<!--  -->
